import ResponsesPage from "@/components/responses/ResponsesPage";

export const metadata = {
    title: "Scrolls — Responses",
};

export default function Responses() {
    return <ResponsesPage />;
}
