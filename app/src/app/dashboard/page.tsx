import DashboardPage from "@/components/dashboard/DashboardPage";

export const metadata = {
    title: "Dashboard — Scrolls",
};

export default function Dashboard() {
    return <DashboardPage />;
}
